---
services:
  db:
    image: postgres:${POSTGRES_VERSION}
    container_name: auction-site-db
    restart: always
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - ./pg_data:/var/lib/postgresql/data

  api:
    build:
      context: ./auction-site-api
      dockerfile: Dockerfile
      args:
        DOTNET_VERSION: ${DOTNET_VERSION}
        API_PORT: ${API_PORT}
    container_name: auction-site-api
    restart: always
    ports:
      - "${API_PORT}:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
      # The Host is 'db', matching the service name above
      - ConnectionStrings__DefaultConnection=${DB_CONNSTRING}
    depends_on:
      - auction-site-db

  app:
    build:
      context: ./auction-site-app
      dockerfile: Dockerfile
      args:
        NODE_VERSION: ${NODE_VERSION}
        NGINX_VERSION: ${NGINX_VERSION}
        APP_PORT: ${APP_PORT}
    container_name: auction-site-app
    restart: always
    env_file:
      - .env
    ports:
      - "${APP_PORT}:80" # Maps local 3000 to Nginx 80
    depends_on:
      - auction-site-api
